# Production frontend config. These are all PUBLIC values (baked into the client
# bundle by design) — NOT secrets — so they live in the repo. Vite auto-loads
# this file on `vite build` (mode=production), so any build (Cloudflare, local,
# CI) gets them without needing host/dashboard env vars configured.
VITE_BACKEND_URL=https://api.bunkermath.az
VITE_DEVELOPMENT_STATUS=production
VITE_GOOGLE_CLIENT_ID=160229370003-bveitv27h5l0v4e82p3jsc7cca664jn0.apps.googleusercontent.com
VITE_CLOUD_NAME=dtuytmw5w
VITE_UPLAD_PRESET=examapp
